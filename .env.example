# Local development
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"
BETTER_AUTH_SECRET="generate-with-openssl-rand-base64-32"
BETTER_AUTH_URL="http://localhost:5173"

# Vercel production (set in Project → Settings → Environment Variables)
# DATABASE_URL= same Neon URL (use the *pooler* host)
# BETTER_AUTH_SECRET= same secret as local (or a dedicated prod secret)
# BETTER_AUTH_URL=https://boardly-r3xr.vercel.app

LIVEBLOCKS_SECRET_KEY=""
